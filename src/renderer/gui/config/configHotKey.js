import ISO from 'modules/app/ISO'

/**
 * 例子：
 * list.push(...[
 *   [['ctrl+w'], GUIEvents.CLOSE_SELF]
 * ])
 *
 */
let list = [
]
if (ISO.isMac) {
} else {
}
let configHotKey = new Map(list)
export default configHotKey
